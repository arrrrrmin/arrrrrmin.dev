{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "local-build": "rm -f src/.observablehq/cache/embeds/* && observable build",
    "serve-build": "npx serve dist",
    "build": "observable build",
    "dev": "observable preview",
    "deploy": "observable deploy",
    "observable": "observable"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.4",
    "@observablehq/stdlib": "^5.8.8",
    "d3": "^7.9.0",
    "sharp": "^0.34.5",
    "topojson-client": "^3.1.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
